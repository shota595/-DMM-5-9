<%= render 'books/list', book: @book, user: @user %>

<div class="col-xs-9">
  <h1>Books</h1>
  <table bgcolor="#fafafa" class="table table-striped">
    <thead>
      <tr>
        <th>img</th>
        <th>Title</th>
        <th>Opinion</th>
      </tr>
    </thead>
    <tbody id="book-index">
      <% @books.each do |book| %>
        <tr class="table-list">
          <%= render 'users/show', user: @user, book: book%>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
